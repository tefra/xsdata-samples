[tox]
envlist = amadeus,sabre,travelport,bnm,common-types,reqif
skipsdist = true

[testenv]
envdir = {toxinidir}/.env
basepython = python3.7
deps = -rrequirements.txt

[testenv:amadeus]
skip_install = true
commands =
    xsdata amadeus/schemas --package amadeus.models
    pytest amadeus

[testenv:sabre]
skip_install = true
commands =
	xsdata sabre/schemas --package sabre.models
    pytest sabre


[testenv:travelport]
commands =
    xsdata travelport/schemas/air_v48_0/Air.wsdl --package travelport.models --wsdl --ns-struct
    pytest travelport

[testenv:bnm]
commands =
    pytest bnm


[testenv:common-types]
commands =
    xsdata common_types/Common-Types/src/main/resources/schemas/HL7V3/NE2008/multicacheschemas --package common_types.models
    mypy common_types

[testenv:reqif]
commands =
    xsdata reqif/schemas/reqif.xsd --package reqif.models --ns-struct
    mypy reqif/models
    pytest reqif
